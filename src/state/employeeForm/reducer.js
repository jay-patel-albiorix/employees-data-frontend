import {
} from '../action-types'

const initialState = {
}

export const reducer = (state = initialState, { type, payload }) => {
    switch(type) {
        default:
            return state
    }
}

export default reducer